# Host port exposed by Docker Compose and Apple Container.
# Leave unset or empty to use 8787.
PORT=8787

# Optional OIDC client secret used by Docker Compose when OIDC is enabled.
OIDC_CLIENT_SECRET=
